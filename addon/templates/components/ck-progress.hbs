{{#each items as |item|}}
  {{log item.value}}
  {{pk-progress-bar max=max min=min
                    value=item.value
                    label=item.label percent=item.percent
                    striped=item.striped active=item.active type=item.type}}
{{/each}}
